# Пошаговый туториал: Утилита копирования файлов

Этот туториал поможет вам самостоятельно реализовать функцию `Copy` от самых базовых концепций до полного решения.

## 📋 Задача (кратко)

Реализовать функцию `Copy(fromPath, toPath string, offset, limit int64) error`, которая:
- Копирует файл из `fromPath` в `toPath`
- Начинает чтение с байта `offset`
- Копирует максимум `limit` байт (если 0 - весь файл)
- Выводит прогресс в процентах
- Возвращает ошибки для невалидных ситуаций

---

## 🎯 Шаг 1: Самое простое - открыть файл для чтения

**Цель**: Просто открыть исходный файл и понять, что это работает.

### Задача:
```go
// В функции Copy():
// 1. Откройте файл fromPath для чтения используя os.Open()
// 2. Обязательно закройте его в конце (используйте defer)
// 3. Проверьте ошибку открытия

// Подсказка: os.Open() возвращает (*os.File, error)
```

### Вопросы для размышления:
- Что произойдет, если файл не существует?
- Почему важно закрывать файл? Что будет, если не закрыть?
- Что делает `defer`? Почему его ставят сразу после проверки ошибки?

### Проверка:
Напишите код, который просто открывает файл. Добавьте временный `fmt.Println("Файл открыт!")` для проверки.

---

## 🎯 Шаг 2: Получить информацию о файле

**Цель**: Узнать размер файла. Это нужно для валидации offset.

### Задача:
```go
// После открытия файла:
// 1. Получите FileInfo используя метод .Stat() у *os.File
// 2. Из FileInfo получите размер файла через .Size()
// 3. Проверьте ошибку от .Stat()

// Подсказка: file.Stat() возвращает (FileInfo, error)
// Подсказка: fileInfo.Size() возвращает int64
```

### Вопросы для размышления:
- Почему размер файла важен для проверки offset?
- Что если файл очень большой? Есть ли ограничения?

### Проверка:
Выведите размер файла в консоль и убедитесь, что получаете корректное значение.

---

## 🎯 Шаг 3: Валидация - проверка offset

**Цель**: Убедиться, что offset не больше размера файла.

### Задача:
```go
// После получения размера файла:
// 1. Сравните offset с размером файла
// 2. Если offset >= размер файла, верните ошибку ErrOffsetExceedsFileSize
// 3. Обратите внимание: в условии сказано "offset больше", но обычно >=

// Вопрос: offset может быть равен размеру файла?
// Ответ: Если offset == размер, то читать нечего, это тоже невалидная ситуация.
```

### Вопросы для размышления:
- Почему offset не может быть больше или равен размеру файла?
- Что будет, если offset = 0? Это валидная ситуация?

### Проверка:
Добавьте проверку. Попробуйте передать offset больше размера файла - должна вернуться ошибка.

---

## 🎯 Шаг 4: Сместиться на offset байт

**Цель**: Начать читать файл не с начала, а с указанной позиции.

### Задача:
```go
// После валидации offset:
// 1. Используйте метод .Seek(offset, whence) у *os.File
// 2. whence = io.SeekStart означает отсчет от начала файла
// 3. Проверьте ошибку от .Seek()
// 4. Проверьте возвращаемую позицию (она должна быть равна offset)

// Подсказка: file.Seek(offset, io.SeekStart) возвращает (int64, error)
```

### Вопросы для размышления:
- Что означает "позиция в файле"? Это как курсор?
- Что произойдет, если мы вызовем Seek с offset = 0?
- Зачем нужна вторая проверка позиции после Seek?

### Проверка:
После Seek выведите текущую позицию - она должна совпадать с offset.

---

## 🎯 Шаг 5: Вычислить сколько байт нужно копировать

**Цель**: Определить реальное количество байт для копирования с учетом limit и остатка файла.

### Задача:
```go
// После Seek:
// 1. Вычислите оставшийся размер: размер_файла - offset
// 2. Если limit == 0, значит копируем все (до конца файла)
// 3. Если limit > 0, копируем минимум из (limit, оставшийся_размер)

// Вопрос: Почему минимум? 
// Ответ: Если limit больше, чем осталось файла, 
//        мы не можем скопировать больше, чем есть.
```

### Вопросы для размышления:
- Что если limit = 0? Что это значит?
- Что если limit = 1000, а осталось 500 байт? Сколько скопируем?
- Что если limit = 100, а осталось 10000 байт? Сколько скопируем?

### Проверка:
Проверьте вычисление для разных сценариев:
- offset=0, limit=0 → должно быть = размер_файла
- offset=100, limit=0 → должно быть = размер_файла - 100
- offset=0, limit=50, размер=1000 → должно быть = 50
- offset=500, limit=1000, размер=1000 → должно быть = 500 (не 1000!)

---

## 🎯 Шаг 6: Создать файл для записи

**Цель**: Открыть/создать файл назначения для записи.

### Задача:
```go
// Перед копированием:
// 1. Создайте файл toPath используя os.Create()
// 2. Не забудьте закрыть его через defer
// 3. Проверьте ошибку создания

// Подсказка: os.Create() автоматически создает файл или обрезает существующий
// Подсказка: os.Create() возвращает (*os.File, error)
```

### Вопросы для размышления:
- Что произойдет, если файл уже существует?
- Что произойдет, если директории для файла не существует?
- Нужно ли проверять, существует ли файл перед созданием?

### Проверка:
После создания файла выведите сообщение об успехе.

---

## 🎯 Шаг 7: Базовое копирование без прогресса

**Цель**: Скопировать данные из одного файла в другой.

### Задача:
```go
// Теперь самое простое копирование:
// 1. Используйте io.CopyN(dst, src, количество_байт)
//    где dst = файл_для_записи, src = файл_для_чтения
// 2. Проверьте ошибку от CopyN
// 3. Проверьте количество скопированных байт

// Подсказка: io.CopyN(dst, src, n) копирует максимум n байт
// Подсказка: Возвращает (int64, error) - количество скопированных байт
```

### Вопросы для размышления:
- Почему CopyN идеально подходит для нашей задачи?
- Что если CopyN вернет меньше байт, чем мы просили? Это ошибка?
- Как это работает "под капотом"? Как данные перемещаются?

### Проверка:
Запустите копирование. Проверьте размер созданного файла - он должен совпадать с вычисленным количеством байт.

---

## 🎯 Шаг 8: Проверка на unsupported file

**Цель**: Определить, можно ли узнать размер файла (для устройств вроде /dev/urandom это невозможно).

### Задача:
```go
// После file.Stat():
// 1. Проверьте, является ли файл обычным файлом
// 2. Используйте fileInfo.Mode() и os.ModeType для проверки
// 3. Если это не обычный файл (например, устройство), 
//    верните ErrUnsupportedFile

// Подсказка: fileInfo.Mode() & os.ModeType == 0 означает обычный файл
// Подсказка: Можно также проверить fileInfo.Mode().IsRegular()
```

### Вопросы для размышления:
- Почему нельзя копировать из /dev/urandom?
- Какие еще типы файлов могут быть необычными?
- Когда это проверять - до или после Seek?

### Проверка:
Попробуйте скопировать из /dev/urandom (если есть доступ) - должна вернуться ошибка.

---

## 🎯 Шаг 9: Прогресс-бар (базовая версия с библиотекой)

**Цель**: Показать прогресс копирования в процентах.

### Задача:
```go
// Вместо простого io.CopyN нужно использовать обертку с прогресс-баром:
// 1. Создайте progress bar используя pb.StartNew(количество_байт)
// 2. Оберните файл_для_чтения в pb.NewReader(file)
// 3. Используйте io.CopyN(dst, progressReader, количество_байт)
// 4. После копирования вызовите progressBar.Finish()

// Подсказка: Импортируйте "github.com/cheggaaa/pb/v3"
// Подсказка: pb.StartNew(total) создает прогресс-бар
// Подсказка: bar.NewReader(file) оборачивает Reader для отслеживания
```

### Вопросы для размышления:
- Как прогресс-бар узнает о прогрессе?
- Почему оборачиваем именно Reader, а не Writer?
- Когда обновляется прогресс - после каждого байта или пакетами?

### Проверка:
Запустите копирование большого файла - вы должны видеть прогресс в процентах.

---

## 🎯 Шаг 10: Правильный порядок операций

**Цель**: Убедиться, что все проверки и операции выполняются в правильном порядке.

### Правильный порядок:
1. ✅ Открыть исходный файл (чтение)
2. ✅ Получить FileInfo и размер
3. ✅ Проверить, что это обычный файл (unsupported)
4. ✅ Проверить offset (не превышает размер)
5. ✅ Выполнить Seek на offset
6. ✅ Вычислить количество байт для копирования
7. ✅ Создать файл назначения (запись)
8. ✅ Создать прогресс-бар
9. ✅ Обернуть Reader в progress reader
10. ✅ Выполнить копирование (CopyN)
11. ✅ Закрыть прогресс-бар
12. ✅ Закрыть файлы (defer сделает это автоматически)

### Вопросы для размышления:
- Почему важно сначала проверить размер, потом offset?
- Можно ли проверить unsupported до открытия файла?
- В каком порядке закрывать файлы? Важно ли?

---

## 🎯 Шаг 11: Написание тестов

**Цель**: Написать тесты для всех сценариев.

### Сценарии для тестирования:

1. **Базовое копирование** (offset=0, limit=0)
   - Копирует весь файл

2. **Копирование с limit** (offset=0, limit=10)
   - Копирует только первые 10 байт

3. **Копирование с offset** (offset=100, limit=1000)
   - Копирует 1000 байт начиная с позиции 100

4. **Limit больше остатка** (offset=500, limit=10000, размер=1000)
   - Копирует только то, что осталось (500 байт)

5. **Ошибка: offset превышает размер**
   - offset >= размер файла → ErrOffsetExceedsFileSize

6. **Ошибка: файл не существует**
   - fromPath не существует → обычная ошибка os.Open

7. **Ошибка: unsupported file** (опционально)
   - /dev/urandom → ErrUnsupportedFile

### Подсказки для тестов:
```go
// Создание временного файла:
tmpFile, err := os.CreateTemp("", "test-*.txt")
defer os.Remove(tmpFile.Name()) // очистка после теста

// Запись тестовых данных:
tmpFile.WriteString("test data")
tmpFile.Close()

// Чтение результата для проверки:
result, err := os.ReadFile(resultPath)
// Сравнение с эталоном:
expected := []byte("expected content")
if !bytes.Equal(result, expected) { ... }
```

---

## 🎯 Шаг 12: Дополнительное задание - свой прогресс-бар

**Цель**: Реализовать прогресс-бар без внешних библиотек.

### Идея:
Вместо `io.CopyN` использовать `io.Copy` с кастомным Writer, который:
1. Записывает данные в файл
2. Отслеживает количество записанных байт
3. Периодически выводит процент выполнения

### Подход:
```go
// Создать структуру, реализующую io.Writer:
type progressWriter struct {
    writer   io.Writer
    total    int64
    written  int64
}

// Реализовать метод Write([]byte) (int, error)
// В котором:
// 1. Записать данные в writer
// 2. Увеличить счетчик written
// 3. Вычислить процент: (written * 100) / total
// 4. Вывести процент (можно с \r для перезаписи строки)
```

### Вопросы для размышления:
- Как часто обновлять прогресс? После каждого Write? Или раз в N байт?
- Как избежать мерцания в консоли?
- Как правильно выводить процент (fmt.Printf с \r)?

---

## 📝 Чек-лист перед сдачей

- [ ] Функция Copy реализована и компилируется
- [ ] Все проверки (offset, unsupported) работают
- [ ] Прогресс-бар показывает процент
- [ ] Тесты покрывают основные сценарии
- [ ] Тесты проходят успешно
- [ ] `test.sh` проходит без ошибок
- [ ] Код чистый и понятный
- [ ] Нет утечек ресурсов (все файлы закрыты)

---

## 🎓 Итоговые вопросы для понимания

1. **Почему мы используем `io.CopyN`, а не просто читаем в буфер и пишем?**
   - Ответ: `io.CopyN` эффективно работает с любыми размерами данных, автоматически использует буферизацию, и код получается проще.

2. **Что происходит, если во время копирования произойдет ошибка?**
   - Ответ: `io.CopyN` вернет ошибку, мы должны ее обработать и вернуть из функции. Файлы закроются через `defer`.

3. **Почему `limit = 0` означает "копировать все"?**
   - Ответ: Это удобное соглашение. Когда limit не задан (0), это означает "без ограничений" - копируем до конца файла.

4. **Зачем нужен Seek перед копированием?**
   - Ответ: Файл открывается на позиции 0. Чтобы начать читать с offset, нужно "перепрыгнуть" туда через Seek.

---

## 🚀 Следующие шаги

1. Начните с Шага 1 - просто откройте файл
2. Проверьте каждую часть отдельно
3. Постепенно добавляйте функциональность
4. Пишите тесты параллельно с кодом
5. Запускайте `go test -v` после каждого шага

**Удачи! Вы справитесь! 💪**




