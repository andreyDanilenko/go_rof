# Практические упражнения для самостоятельной работы

Эти упражнения помогут вам проверить понимание каждого этапа.

## 🔍 Упражнение 1: Анализ требований

**Задача**: Проанализируйте требования и заполните таблицу:

| Сценарий | offset | limit | Размер файла | Ожидаемое поведение |
|----------|--------|-------|--------------|---------------------|
| 1 | 0 | 0 | 1000 | ? |
| 2 | 0 | 100 | 1000 | ? |
| 3 | 100 | 500 | 1000 | ? |
| 4 | 500 | 1000 | 1000 | ? |
| 5 | 1000 | 100 | 1000 | ? |
| 6 | 1001 | 100 | 1000 | ? |
| 7 | 500 | 0 | 1000 | ? |

**Ваши ответы** (заполните):
1. _________________________________
2. _________________________________
3. _________________________________
4. _________________________________
5. _________________________________
6. _________________________________
7. _________________________________

---

## 🔍 Упражнение 2: Псевдокод

**Задача**: Напишите псевдокод функции Copy БЕЗ использования конкретного синтаксиса Go:

```
ФУНКЦИЯ Copy(fromPath, toPath, offset, limit):
    1. _________________________________
    2. _________________________________
    3. _________________________________
    4. _________________________________
    5. _________________________________
    6. _________________________________
    7. _________________________________
    8. _________________________________
```

**Подсказка**: Используйте простые слова: "открыть", "получить", "проверить", "вычислить", "скопировать"

---

## 🔍 Упражнение 3: Поиск в документации

**Задача**: Найдите в документации Go ответы на вопросы:

1. Что возвращает `os.Open()`?
   - Ответ: _________________________________

2. Что делает `file.Seek(offset, whence)`?
   - Ответ: _________________________________

3. Что означают константы `io.SeekStart`, `io.SeekCurrent`, `io.SeekEnd`?
   - Ответ: _________________________________

4. Что делает `io.CopyN(dst, src, n)`?
   - Ответ: _________________________________

5. Как проверить, является ли файл обычным файлом?
   - Ответ: _________________________________

**Подсказка**: Используйте `go doc` команду или https://pkg.go.dev/

---

## 🔍 Упражнение 4: Отладка ментально

**Задача**: Представьте, что у вас есть такой код. Найдите ошибки:

```go
func Copy(fromPath, toPath string, offset, limit int64) error {
    src, err := os.Open(fromPath)
    if err != nil {
        return err
    }
    
    size := src.Size()  // ОШИБКА 1
    if offset > size {  // ОШИБКА 2
        return ErrOffsetExceedsFileSize
    }
    
    src.Seek(offset, 0)  // ОШИБКА 3
    
    dst, err := os.Open(toPath)  // ОШИБКА 4
    if err != nil {
        return err
    }
    
    copySize := limit  // ОШИБКА 5
    io.CopyN(dst, src, copySize)
    
    return nil
}
```

**Найденные ошибки**:
1. _________________________________
2. _________________________________
3. _________________________________
4. _________________________________
5. _________________________________
6. (есть еще ошибки - найдите их!)

---

## 🔍 Упражнение 5: Вычисление размера копирования

**Задача**: Вычислите, сколько байт будет скопировано в каждом случае:

```go
fileSize := 6617  // размер testdata/input.txt

// Случай 1:
offset := 0
limit := 0
copySize := ?  // Ваш ответ: _____

// Случай 2:
offset := 0
limit := 100
copySize := ?  // Ваш ответ: _____

// Случай 3:
offset := 100
limit := 1000
copySize := ?  // Ваш ответ: _____

// Случай 4:
offset := 5000
limit := 10000
copySize := ?  // Ваш ответ: _____

// Случай 5:
offset := 6000
limit := 1000
copySize := ?  // Ваш ответ: _____
```

**Формула**: 
```
остаток = fileSize - offset
если limit == 0:
    copySize = остаток
иначе:
    copySize = минимум(limit, остаток)
```

---

## 🔍 Упражнение 6: Написание тестовых случаев

**Задача**: Напишите тестовые функции для следующих сценариев:

### Тест 1: Копирование всего файла
```go
func TestCopyFullFile(t *testing.T) {
    // TODO: Напишите тест
    // 1. Создайте временный исходный файл с содержимым "Hello, World!"
    // 2. Вызовите Copy с offset=0, limit=0
    // 3. Проверьте, что файл назначения содержит "Hello, World!"
    // 4. Удалите временные файлы
}
```

### Тест 2: Копирование с limit
```go
func TestCopyWithLimit(t *testing.T) {
    // TODO: Напишите тест
    // 1. Создайте файл с содержимым "Hello, World!"
    // 2. Вызовите Copy с offset=0, limit=5
    // 3. Проверьте, что скопировалось только "Hello"
}
```

### Тест 3: Копирование с offset
```go
func TestCopyWithOffset(t *testing.T) {
    // TODO: Напишите тест
    // 1. Создайте файл с содержимым "Hello, World!"
    // 2. Вызовите Copy с offset=7, limit=5
    // 3. Проверьте, что скопировалось "World"
}
```

### Тест 4: Ошибка - offset превышает размер
```go
func TestCopyOffsetExceedsSize(t *testing.T) {
    // TODO: Напишите тест
    // 1. Создайте файл размером 10 байт
    // 2. Вызовите Copy с offset=100
    // 3. Проверьте, что вернулась ошибка ErrOffsetExceedsFileSize
}
```

**Подсказки**:
- Используйте `os.CreateTemp()` для создания временных файлов
- Используйте `os.ReadFile()` для чтения результата
- Используйте `bytes.Equal()` для сравнения байтов
- Используйте `os.Remove()` или `defer os.Remove()` для очистки

---

## 🔍 Упражнение 7: Пошаговая отладка

**Задача**: Запустите этот код и объясните, что выводится на каждом шаге:

```go
func DebugCopy() {
    file, _ := os.Open("testdata/input.txt")
    defer file.Close()
    
    info, _ := file.Stat()
    size := info.Size()
    fmt.Printf("1. Размер файла: %d\n", size)
    
    pos1, _ := file.Seek(0, io.SeekCurrent)
    fmt.Printf("2. Текущая позиция: %d\n", pos1)
    
    pos2, _ := file.Seek(100, io.SeekStart)
    fmt.Printf("3. Позиция после Seek(100): %d\n", pos2)
    
    offset := 100
    limit := 1000
    remaining := size - int64(offset)
    copySize := limit
    if limit == 0 || limit > remaining {
        copySize = int(remaining)
    }
    fmt.Printf("4. Будет скопировано: %d байт\n", copySize)
}
```

**Ожидаемый вывод** (заполните после запуска):
1. _________________________________
2. _________________________________
3. _________________________________
4. _________________________________

---

## 🔍 Упражнение 8: Понимание io.CopyN

**Задача**: Ответьте на вопросы о `io.CopyN`:

1. Что произойдет, если вы вызовете `io.CopyN(dst, src, 100)`, но в `src` осталось только 50 байт?
   - Ответ: _________________________________

2. Что произойдет, если вы вызовете `io.CopyN(dst, src, 0)`?
   - Ответ: _________________________________

3. Может ли `io.CopyN` скопировать меньше байт, чем запрошено? Когда?
   - Ответ: _________________________________

4. Что означает вторая возвращаемая величина от `io.CopyN`?
   - Ответ: _________________________________

**Проверка**: Напишите маленькую программу, чтобы проверить свои ответы.

---

## 🔍 Упражнение 9: Прогресс-бар (концепция)

**Задача**: Представьте, что вы копируете файл размером 1000 байт порциями по 100 байт. Заполните таблицу:

| Скопировано | Всего | Процент | Как вычислить? |
|-------------|-------|---------|----------------|
| 0 | 1000 | ? | |
| 100 | 1000 | ? | |
| 200 | 1000 | ? | |
| 500 | 1000 | ? | |
| 1000 | 1000 | ? | |

**Формула процента**: 
```
процент = (скопировано * 100) / всего
```

---

## 🔍 Упражнение 10: Финальная проверка понимания

**Задача**: Ответьте на вопросы:

1. **Почему важно закрывать файлы?**
   - Ответ: _________________________________

2. **В каком порядке должны выполняться операции? Почему?**
   - Ответ: _________________________________

3. **Что произойдет, если не проверить offset перед Seek?**
   - Ответ: _________________________________

4. **Почему limit=0 означает "копировать все", а не "не копировать ничего"?**
   - Ответ: _________________________________

5. **Как прогресс-бар узнает о прогрессе копирования?**
   - Ответ: _________________________________

---

## ✅ Самопроверка

После выполнения всех упражнений проверьте себя:

- [ ] Я понимаю, что делает каждая функция из стандартной библиотеки
- [ ] Я могу объяснить, почему операции выполняются в таком порядке
- [ ] Я понимаю, как вычисляется количество байт для копирования
- [ ] Я знаю, какие ошибки нужно обрабатывать и почему
- [ ] Я понимаю, как работает прогресс-бар
- [ ] Я могу написать тесты для всех сценариев
- [ ] Я готов самостоятельно реализовать функцию Copy

---

## 🎯 Следующий шаг

После выполнения упражнений переходите к реализации функции Copy, следуя шагам из TUTORIAL.md.

**Удачи! 💪**





